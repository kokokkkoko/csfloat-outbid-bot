# 🚀 Quick Start Guide

Быстрый старт для CSFloat Outbid Bot.

## За 5 минут

### 1. Установка (2 минуты)

```bash
# Перейдите в директорию проекта
cd /d/projects/bot

# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Создайте директорию для логов
mkdir logs
```

### 2. Настройка (1 минута)

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Или создайте .env вручную с содержимым:
# HOST=0.0.0.0
# PORT=8000
# CHECK_INTERVAL=120
# OUTBID_STEP=0.01
# MAX_OUTBIDS=10
```

### 3. Запуск (30 секунд)

```bash
python main.py
```

Откройте браузер: **http://localhost:8000**

### 4. Первоначальная настройка (1-2 минуты)

1. **Добавьте аккаунт:**
   - Нажмите "Add Account"
   - Введите название, API ключ CSFloat
   - (Опционально) Добавьте прокси
   - Нажмите "Save"

2. **Проверьте подключение:**
   - Нажмите иконку ✓ рядом с аккаунтом
   - Убедитесь что статус стал 🟢 online

3. **Настройте параметры:**
   - Check Interval: 120-180 секунд (рекомендуется)
   - Outbid Step: $0.01-$0.05
   - Max Outbids: 5-10

4. **Создайте buy orders на CSFloat:**
   - Зайдите на сайт CSFloat
   - Создайте buy orders как обычно
   - Бот автоматически обнаружит их

5. **Запустите бота:**
   - Нажмите кнопку **"Start"**
   - Готово! 🎉

## ⚠️ Важно перед использованием

### Advanced Buy Orders НЕ РАБОТАЮТ из коробки!

Для работы с advanced orders (с указанием float) нужно:

1. Открыть CSFloat в браузере
2. Открыть DevTools (F12) → Network
3. Создать advanced buy order
4. Найти POST запрос и скопировать:
   - URL эндпоинта
   - Формат JSON payload
5. Исправить в файле `bot/advanced_api.py`

**Подробности в README.md, раздел "Advanced Buy Orders"**

## 📊 Что дальше?

- Следите за разделом "Active Orders" - там отображаются отслеживаемые ордера
- В "Outbid History" видна история перебивов
- Статусы аккаунтов обновляются автоматически

## 🐛 Проблемы?

### Ошибка при запуске

```bash
# Убедитесь что активировали venv:
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# Переустановите зависимости:
pip install -r requirements.txt --upgrade
```

### Бот не видит ордера

Бот пока НЕ создает ордера автоматически. Создайте их вручную на CSFloat, затем бот начнет их отслеживать.

### Другие проблемы

Смотрите логи: `logs/bot_YYYY-MM-DD.log`

---

**Готово! Теперь бот автоматически перебивает конкурентов! 🚀**
