# 🚀 Быстрый старт для друзей

## Шаг 1: Установить Docker Desktop

Скачай и установи Docker Desktop:
- **Windows**: https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe
- **Mac**: https://desktop.docker.com/mac/main/amd64/Docker.dmg

После установки запусти Docker Desktop (иконка кита должна появиться в трее).

---

## Шаг 2: Скачать бота

Открой **командную строку** (CMD) или **PowerShell** и выполни:

```bash
git clone https://github.com/kokokkkoko/csfloat-outbid-bot.git
cd csfloat-outbid-bot
```

Если нет Git, скачай: https://git-scm.com/downloads

---

## Шаг 3: Настроить

Скопируй файл с настройками:

**Windows (CMD):**
```bash
copy .env.example .env
```

**Windows (PowerShell):**
```bash
Copy-Item .env.example .env
```

**Mac/Linux:**
```bash
cp .env.example .env
```

Открой файл `.env` в блокноте и измени `JWT_SECRET_KEY`:

```bash
notepad .env
```

Замени строку:
```
JWT_SECRET_KEY=change-this-in-production
```

На свой секретный ключ (любая длинная строка):
```
JWT_SECRET_KEY=мой-секретный-ключ-12345-абвгд-67890
```

**Сохрани и закрой.**

---

## Шаг 4: Запустить

Убедись что **Docker Desktop запущен**, затем выполни:

```bash
docker-compose up -d
```

Подожди 1-3 минуты пока скачается образ и запустится контейнер.

---

## Шаг 5: Открыть веб-интерфейс

Открой браузер и перейди по адресу:

```
http://localhost:8000
```

---

## Шаг 6: Зарегистрироваться

1. Нажми **"Register"**
2. Введи логин, email и пароль
3. Войди в систему

---

## Шаг 7: Добавить CSFloat аккаунт

1. Получи API ключ на https://csfloat.com/api-key
2. В боте нажми **"Accounts"** → **"Add Account"**
3. Введи:
   - **Name:** любое имя (например "Main")
   - **API Key:** твой ключ от CSFloat
   - **Proxy:** оставь пустым
4. Нажми **"Save"**
5. Нажми **"Test"** чтобы проверить подключение
6. Нажми **"Sync"** чтобы загрузить buy orders

---

## Шаг 8: Запустить бота

Нажми кнопку **"Start Bot"** вверху страницы.

**Готово! 🎉** Бот начнет автоматически перебивать конкурентов.

---

## 📝 Полезные команды

### Остановить бота:
```bash
docker-compose down
```

### Посмотреть логи:
```bash
docker-compose logs -f
```

### Обновить до новой версии:
```bash
git pull
docker-compose down
docker-compose up -d --build
```

### Перезапустить бота:
```bash
docker-compose restart
```

---

## ❓ Проблемы?

### "Cannot connect to Docker daemon"
→ Убедись что Docker Desktop запущен (иконка кита в трее)

### "Port 8000 already in use"
→ Измени порт в `docker-compose.yml`: `"8001:8000"` вместо `"8000:8000"`

### "Connection refused" при тесте аккаунта
→ Проверь правильность API ключа CSFloat

### Забыл пароль
→ Удали базу данных и зарегистрируйся заново:
```bash
docker-compose down
rm -rf data/bot.db  # или del data\bot.db на Windows
docker-compose up -d
```

---

## 🎯 Что дальше?

- **Settings** - настрой интервал проверки и шаг перебивания
- **Orders** - посмотри свои активные buy orders
- **History** - история всех перебивов

---

## 📚 Дополнительно

- Полная документация: [README.md](README.md)
- Подробная инструкция: [INSTALL.md](INSTALL.md)

**Удачи! 🚀**
