# CSFloat Outbid Bot

Полноценный торговый бот для автоматического управления buy-ордерами на CSFloat с функцией автоматического перебивания (outbidding) конкурентов.

## 🚀 Возможности

- ✅ Поддержка до 3 аккаунтов одновременно
- ✅ Автоматическое перебивание более высоких buy orders
- ✅ Поддержка simple и advanced buy orders (с указанием float диапазона)
- ✅ Гибкие настройки: интервал проверки, шаг перебивания, лимиты
- ✅ Защита от бесконечного перебития
- ✅ Поддержка прокси для каждого аккаунта (SOCKS5/HTTP)
- ✅ Удобный веб-интерфейс
- ✅ История всех перебивов
- ✅ Асинхронная архитектура для максимальной производительности

## 📋 Требования

- Python 3.10 или выше
- CSFloat API ключ(и)
- (Опционально) Прокси-серверы

## 🛠️ Установка

### 1. Клонируйте репозиторий или распакуйте архив

```bash
cd /d/projects/bot
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте конфигурацию

Скопируйте файл `.env.example` в `.env` и отредактируйте по необходимости:

```bash
cp .env.example .env
```

Пример `.env`:
```env
HOST=0.0.0.0
PORT=8000
DATABASE_URL=sqlite+aiosqlite:///./bot.db
CHECK_INTERVAL=120
OUTBID_STEP=0.01
MAX_OUTBIDS=10
LOG_LEVEL=INFO
```

### 5. Создайте директорию для логов

```bash
mkdir logs
```

## 🚀 Запуск

```bash
python main.py
```

Веб-интерфейс будет доступен по адресу: **http://localhost:8000**

## 📖 Использование

### 1. Добавьте аккаунты

1. Откройте веб-интерфейс
2. Нажмите "Add Account"
3. Введите:
   - **Name**: Название аккаунта (например, "Main Account")
   - **API Key**: Ваш CSFloat API ключ
   - **Proxy** (опционально): `http://user:pass@ip:port` или `socks5://user:pass@ip:port`

### 2. Настройте параметры бота

В разделе "Settings" укажите:
- **Check Interval**: Интервал проверки ордеров (в секундах, рекомендуется 120-180)
- **Outbid Step**: Шаг перебивания в долларах (например, 0.01-0.05)
- **Max Outbids**: Максимальное количество перебивов на один ордер

### 3. Создайте buy orders на CSFloat

Создайте buy orders через веб-интерфейс CSFloat или мобильное приложение. Бот автоматически обнаружит их и начнет отслеживать.

### 4. Запустите бота

Нажмите кнопку **"Start"** в веб-интерфейсе.

## ✅ Advanced Buy Orders - РАБОТАЕТ!

Бот полностью поддерживает advanced buy orders (с указанием float диапазона)!

### Как это работает:

1. **Создание/удаление** - используется `bot/advanced_api.py` с прямыми HTTP запросами к CSFloat API
2. **Перебивание конкурентов** - бот:
   - Ищет листинги с подходящим float диапазоном (например 0.21-0.24 для FT)
   - Получает buy orders для каждого найденного листинга
   - Фильтрует buy orders по пересечению float диапазонов
   - Находит максимальную цену конкурента и перебивает её

### Важные детали:

- Бот проверяет до 50 листингов для каждого advanced ордера
- Если нет листингов с нужным float - использует любые листинги этого скина (fallback)
- Buy orders дедуплицируются (один ордер может конкурировать за несколько листингов)
- Корректно определяет пересечение float диапазонов между вашим ордером и конкурентами

## 🗂️ Структура проекта

```
bot/
├── main.py                 # Точка входа
├── config.py               # Конфигурация
├── database.py             # SQLAlchemy модели и подключение
├── accounts.py             # Управление аккаунтами
├── requirements.txt        # Зависимости
├── .env                    # Настройки (создать вручную)
├── bot/
│   ├── __init__.py
│   ├── manager.py          # Главный менеджер бота
│   ├── outbid_logic.py     # Логика перебивания
│   └── advanced_api.py     # API для advanced orders (ТРЕБУЕТ ДОРАБОТКИ!)
├── web/
│   ├── app.py              # FastAPI приложение
│   ├── static/
│   │   └── app.js          # Frontend JavaScript
│   └── templates/
│       └── index.html      # HTML интерфейс
└── logs/                   # Логи (создается автоматически)
```

## 🔧 Архитектура

### База данных (SQLite)

- **accounts** - аккаунты CSFloat
- **buy_orders** - активные buy orders
- **outbid_history** - история перебивов

### Модули бота

1. **BotManager** (`bot/manager.py`)
   - Главный координатор
   - Проверяет все аккаунты параллельно
   - Координирует перебивание

2. **OutbidLogic** (`bot/outbid_logic.py`)
   - Логика принятия решений о перебивании
   - Расчет новых цен
   - Запись истории

3. **AdvancedOrderAPI** (`bot/advanced_api.py`)
   - Работа с advanced orders через HTTP
   - **ТРЕБУЕТ ДОРАБОТКИ** - см. раздел выше

4. **AccountManager** (`accounts.py`)
   - Управление аккаунтами
   - Создание CSFloat клиентов
   - Тестирование подключений

## 🔐 Безопасность

- API ключи хранятся в локальной базе данных
- В веб-интерфейсе ключи показываются частично (`xxx...`)
- Используйте `.env` для конфиденциальных настроек
- **НЕ коммитьте** `.env` в Git (уже добавлен в `.gitignore`)

## 📊 Логирование

Логи сохраняются в:
- **Консоль** - цветной вывод с уровнем INFO
- **Файлы** - `logs/bot_YYYY-MM-DD.log` с уровнем DEBUG
- Ротация - ежедневно, хранятся 30 дней

## ⚙️ API Endpoints

### Bot Control
- `POST /api/bot/start` - Запустить бота
- `POST /api/bot/stop` - Остановить бота
- `GET /api/bot/status` - Статус бота

### Accounts
- `GET /api/accounts` - Список аккаунтов
- `POST /api/accounts` - Создать аккаунт
- `PUT /api/accounts/{id}` - Обновить аккаунт
- `DELETE /api/accounts/{id}` - Удалить аккаунт
- `POST /api/accounts/{id}/test` - Тестировать подключение

### Orders
- `GET /api/orders` - Список ордеров
- `DELETE /api/orders/{id}` - Отменить ордер

### History
- `GET /api/history` - История перебивов

### Settings
- `GET /api/settings` - Получить настройки
- `PUT /api/settings` - Обновить настройки

## 🐛 Отладка

### Проблемы с подключением

1. Проверьте API ключ на CSFloat
2. Используйте кнопку "Test" для аккаунта
3. Проверьте логи в `logs/`
4. Убедитесь что прокси (если используется) работает

### Бот не перебивает

1. Проверьте что бот запущен (статус "Running")
2. Проверьте интервал проверки (не слишком большой?)
3. Проверьте лимиты перебивания (не достигнуты?)
4. Посмотрите логи - возможно ошибки API

### Advanced orders не перебивают конкурентов

1. Проверьте что листинги с нужным float диапазоном существуют на рынке
2. Проверьте логи - должны быть сообщения "Found X listings" и "Got Y buy orders"
3. Если бот находит 0 конкурентов - возможно ваш ордер уже топовый!
4. Fallback логика сработает автоматически если нет листингов с точным float

## 📝 TODO / Улучшения

- [x] ~~Уточнить и исправить эндпоинты для advanced orders~~ ✅ ГОТОВО!
- [x] ~~Исправить поиск конкурентов для advanced orders~~ ✅ ГОТОВО!
- [ ] Добавить создание ордеров через веб-интерфейс
- [ ] Добавить уведомления (Telegram, Discord)
- [ ] Добавить статистику и графики
- [ ] Добавить поддержку более 3 аккаунтов
- [ ] Добавить экспорт данных (CSV, JSON)
- [ ] Добавить профили настроек
- [ ] Добавить тесты (pytest)

## 📄 Лицензия

MIT License - используйте как хотите, но на свой риск!

## ⚠️ Disclaimer

Этот бот создан в образовательных целях. Используйте на свой страх и риск. Автор не несет ответственности за:
- Потерю средств
- Бан аккаунтов
- Любые другие проблемы

Убедитесь, что использование ботов не нарушает Terms of Service CSFloat.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/`
2. Проверьте раздел "Отладка" выше
3. Создайте Issue с описанием проблемы и логами

---

**Удачи в трейдинге! 🚀**
